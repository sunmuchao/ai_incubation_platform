/**
 * AI Native 首页 - Chat-first 交互范式
 *
 * 全新 AI Native 界面，核心特性：
 * 1. 对话式交互 - 主界面是 AI 对话窗口
 * 2. Generative UI - 动态生成界面组件
 * 3. Agent 可视化 - AI Agent 状态和声誉展示
 * 4. 主动推送 - 通知和推荐
 */

'use client';

import { AINativeHome } from '@/components/ai-native/AINativeHome';

export default function Home() {
  return <AINativeHome />;
}
